// Gifshot.min.js - Minified version for GIF capture
// Este archivo puede ser reemplazado por la versión remota desde CDN
// https://cdn.jsdelivr.net/npm/gifshot@0.4.5/dist/gifshot.min.js

(function() {
    // Placeholder for gifshot library
    window.gifshot = window.gifshot || {
        createGIF: function(options, callback) {
            console.warn('gifshot: Initialize with CDN or proper library');
            if (callback) callback({ error: true });
        }
    };
})();
